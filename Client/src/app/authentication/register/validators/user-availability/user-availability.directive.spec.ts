import { UserAvailabilityDirective } from './user-availability.directive';

describe('UserAvailabilityDirective', () => {
  it('should create an instance', () => {
    const directive = new UserAvailabilityDirective();
    expect(directive).toBeTruthy();
  });
});
